## 关键字
int float double abstract class ...

## 常量
常量存储在常量区，不会变换的量，一般的有字符常量，枚举，字符常量
使用const 关键字标识 常量名使用大写

## 变量


## 数据类型
### 基本数据类型
| 类型 | 字节 |
| ---- | ---- |
| byte |   1  |
| short|   2  |
| int |   4  |
| float |   4  |
| double |   8  |
| long |   8  |
| char |   1  |
| boolean |   1  |
java中默认整型是int,浮点数是double

### 引用数据类型
string
接口
class
lambda

## 数据类型转换
### 自动转换
低精度转高精度

### 强制类型转换
高精度类型转低精度类型，可以造成数据丢失。

byte b = (byte)12;


## 数组
是同一类型的集合，是一种引用类型。数组定义之后长度不能改变。

数据类型[] 数组名 = new 数据类型[数组长度]

泛型只能是引用类型，不是值类型

重写（Override）：方法名一样，参数一样
重载、覆盖（Overload）：方法名一样，参数不一样


## 多线程
并发：指两个以上的事件在同一个事件段内发生，交替执行
并行：指两个以上的事件在同一个时刻发生

### 进程和线程
- 进程：是指一个程序加载到内存中叫做进程，也是程序


- 线程：是一个进程的一个执行单元，负责程序的执行

